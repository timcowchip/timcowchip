# Description:	A suite of megawidgets for Tk
# URL:		hA suite of megawidgets for Tk
# Maintainer:    Chris Farrell, timcowchip at gorge dot net
# Depends: bash tcl

name=bwidget
version=1.9.10
release=1

source=("http://downloads.sourceforge.net/project/tcllib/BWidget/$version/bwidget-$version.tar.gz")

build() {
  cd "${SRC}"
  install -d "${PKG}"/usr/lib/tcl8.6
  cp -r bwidget-$version "${PKG}"/usr/lib/tcl8.6/
}
