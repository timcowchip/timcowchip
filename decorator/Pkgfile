# Description: decorator (decorator module for Python)
# URL: https://pypi.python.org/pypi/decorator
# Maintainer: Chris Farrell, timcowchip at gorge dot net
# Depends on: python-setuptools

name=decorator
version=4.1.2
release=1
source=(https://files.pythonhosted.org/packages/source/d/$name/$name-$version.tar.gz)

build() {
  cd $name-$version
  python setup.py install --root=$PKG
}
