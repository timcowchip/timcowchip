# Description: open source video transcoder
# URL: http://handbrake.fr/
# Maintainer: Chris Farrell, timcowchip at gmail dot com
# Depends on: faad2 fontconfig freetype fribidi libass libogg libsamplerate libtheora libvorbis libxml2 python yasm x264

name=handbrake
version=1.0.4
release=2
source=(https://handbrake.fr/rotation.php?file=HandBrake-1.0.4.tar.bz2)

build() {
	cd HandBrake-$version
	
	    ./configure \
		--prefix=/usr \
		--force \
		--disable-gtk-update-checks

	cd build
	make -j1
	make DESTDIR=$PKG install
	rm -rf ${PKG}/usr/share/locale/
}
