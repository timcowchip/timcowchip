# Description:	Open source implementation of the OBEX protocol
# URL:		http://dev.zuckschwerdt.org/openobex/
# Maintainer:    Chris Farrell, timcowchip at gorge dot net
# Depends: bluez libical

name=obexd
version=0.48
release=1
source=(https://www.kernel.org/pub/linux/bluetooth/$name-$version.tar.xz / obexd)

build() {
		cd $name-$version
		
		./configure --prefix=/usr --libexecdir=/usr/lib/obex 
		
		make
		
		make DESTDIR=$PKG install
		
		nstall -D -m 0755 $SRC/obexd $PKG/etc/rc.d/obexd
}