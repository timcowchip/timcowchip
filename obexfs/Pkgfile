# Description:	FUSE based filesystem using ObexFTP 
# URL:		http://openobex.triq.net/obexfs
# Maintainer:    Chris Farrell, timcowchip at gorge dot net
# Depends: obexftp fuse

name=obexfs
version=0.12
release=1

source=(https://superb-sea2.dl.sourceforge.net/project/openobex/obexfs/0.12/$name-$version.tar.gz)

build() {
  cd "$SRC"/$name-$version

  export OBEXFTP_CFLAGS="-I/usr/include/obexftp -I/usr/include/multicobex -I/usr/include/bfb"
  export OBEXFTP_LIBS="-lobexftp -lmulticobex -lbfb -lopenobex -lbluetooth"

  ./configure --prefix=/usr
  make

  make DESTDIR="$PKG" install
}
