# Description:	A front-end of obexftp
# URL:		http://obextool.sf.net
# Maintainer:    Chris Farrell, timcowchip at gorge dot net
# Depends: perl obexftp bwidget tablelist tk

name=obextool
version=0.35
release=1

source=(http://www.tech-edv.co.at/TCMS/downloads/obextool-$version.tar.gz)

build() {
  install -d "$PKG"/usr/{share,bin}
  mv "$SRC"/${name}-$version "$PKG"/usr/share/
  cd "$PKG"/usr/bin
  echo "#!/bin/bash" > "$PKG"/usr/bin/obextool
  echo "/usr/share/$name-$version/obextool.tk \"\$@\"" >> "$PKG"/usr/bin/obextool
  chmod +x "$PKG"/usr/bin/obextool
}
