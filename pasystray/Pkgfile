# Description: PulseAudio system tray
# URL: https://github.com/christophgysin/pasystray
# Maintainer: Chris Farrell, timcowchip at gmail dot com
# Depends on: pulseaudio libnotify avahi gnome-icon-theme 

name=pasystray
version=0.6.0
release=1

source=()

build () {

	git clone https://github.com/christophgysin/${name}.git

	cd "$SRC/${name}"
	autoreconf -is
	sed -i -e '/<requires /d' -e '/license_type/d' src/pasystray.gtk3.glade
	# Do you need appindicator in gtk3?
	./configure --prefix=/usr --with-gtk=3 --disable-appindicator

        make DESTDIR="$PKG/" install
}
