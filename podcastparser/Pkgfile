# Description: podcastparser is a podcast parser written in Python
# URL: http://gpodder.org/podcastparser/
# Maintainer: Chris Farrell, timcowchip at gmail dot com
# Depends on: python-setuptools

name=podcastparser
version=0.6.2
release=2
source=(https://github.com/gpodder/podcastparser/archive/$version/$name-$version.tar.gz)

build() {
  cd $name-$version
  python setup.py install --root=$PKG
}
