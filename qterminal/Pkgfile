# Description: Lightweight Qt-based terminal emulator
# URL: https://github.com/qterminal/qterminal
# Maintainer: Chris Farrell, timcowchip at gmail dot com
# Depends on: qt5 qtermwidget

name=qterminal
version=0.8.0
release=2

source=("https://raw.githubusercontent.com/timcowchip/DISTFILES/master/$name-$version.tar.gz")

build() {
	mkdir -p build
	cd build
	cmake "$SRC/$name-$version/home/timcowchip/Downloads/qterminal-0.8.0.tar.gz" \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_BUILD_TYPE=Release \
		-DUSE_SYSTEM_QXT=OFF \
		-DPULL_TRANSLATIONS=no \
		-DUSE_QT5=true
	make

	make DESTDIR="$PKG" install
}
