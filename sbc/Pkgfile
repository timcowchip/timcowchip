# Description: digital audio encoder and decoder used to transfer data to Bluetooth audio output devices like headphones or loudspeakers
# URL: https://www.kernel.org/pub/linux/bluetooth/
# Maintainer: Chris Farrell, timcowchip at gmail dot com
# Depends on: libsndfile

name=sbc
version=1.3
release=1

source=(https://www.kernel.org/pub/linux/bluetooth/sbc-1.3.tar.xz)

build() {
  cd $name-$version
  
  ./configure
  --prefix=/usr\
  --disable-static\
  --disable-tester
    
  make

  make DESTDIR="$PKG" install
}
