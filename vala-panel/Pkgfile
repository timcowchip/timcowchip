# Description: Gtk3 panel for compositing window managers
# URL: https://github.com/rilian-la-te/vala-panel
# Maintainer: Chris Farrell, timcowchip at gmail dot com
# Depends on: gtk3 vala libwnck-gtk3 libpeas gobject-introspection gsettings-desktop-schemas

name=vala-panel
version=0.3.8
release=1

source=()

build() {

  git clone https://github.com/rilian-la-te/vala-panel.git
  
  cd "${SRC}/${name}"
  
  git submodule init && git submodule update


  cmake ./ -DCMAKE_C_FLAGS=-fPIC -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_INSTALL_LIBDIR=lib -DENABLE_WNCK=ON
  make

  cd "${SRC}/${name}"
  make DESTDIR="${PKG}" install
}


