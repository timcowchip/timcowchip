# Description: A weather plugin for the Xfce4 panel
# URL: http://goodies.xfce.org/projects/panel-plugins/xfce4-weather-plugin
# Maintainer: Chris Farrell, timcowchip at gorge dot net
# Depends on: xfce4-panel libxml2 libsoup hicolor-icon-theme

name=xfce4-weather-plugin
version=0.8.10
release=1

source=(https://archive.xfce.org/src/panel-plugins/$name/${version%.*}/$name-$version.tar.bz2)

build() {
  cd "$SRC/$name-$version"

  ./configure \
    --prefix=/usr \
    --sysconfdir=/etc \
    --libexecdir=/usr/lib \
    --localstatedir=/var \
    --disable-static \
    --disable-debug \
    --disable-upower
  make

  make DESTDIR="$PKG" install
}

